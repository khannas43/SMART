# Auth Service - Application Configuration

spring:
  config:
    import: optional:file:../../../../config/application.yml

server:
  port: 8090

logging:
  level:
    com.smart.auth: DEBUG

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:smart-jwt-secret-change-in-production}
  expiration: 3600000  # 1 hour

